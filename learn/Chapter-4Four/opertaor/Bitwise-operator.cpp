/* 位运算符
 * 满足右结合律
 * 作用与整数类型的对象
 * 将对象看成二进制的集合
 * 位求反(~)            位求反，所有的0变为1所有的1变为0
 * 左移右移(<< / >>)    左移将相应的二进制向左移右侧补0 右移超出边线自动消除
 *
 * 位与(&)   位异或(^)   位或(|)
 * 这三个是两两间比较，比较二进制位上的0和1
 * 位与-两个都为1时为1
 * 位或-至少有一个为1 时为1
 * 位异或- 有且只有一个为1时才为1
 */
#include<iostream>
#include<string>
#include<vector>
using namespace std;
int main()
{
    return 0;
}
