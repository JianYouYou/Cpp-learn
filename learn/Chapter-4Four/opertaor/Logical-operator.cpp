/* 逻辑运算符
 *
 */
#include<iostream>
#include<string>
using namespace std;
int main()
{
    /* 逻辑非(!)
     * 将对象取反后返回，真返回假，假返回真
     * 大于
     * 等于
     * 小于
     * 大于等于
     * 小于等于
     * 逻辑与(&&) 左侧为真时才看右边
     * 逻辑或(||) 左侧为假时才看右边
     *
     */

    return 0;
}